#include "main.h"
/**
 *  _isupper - This function returns
 * %c is 1 if is uppercase and 0 otherwise
 *  Return: 1 if is uppercase, 0 otherwise
 */
int _isupper(int c)
{
if (c >= 65 && c < 91)
return (1);
else
return (0);
}
