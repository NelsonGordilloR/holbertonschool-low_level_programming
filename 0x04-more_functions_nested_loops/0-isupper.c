#include "main.h"
/**
 *  _isupper - This function returns 1 if is uppercase and 0 otherwise
 * @c: integer
 *  Return: 1 if is uppercase, 0 otherwise
 */
int _isupper(int c)
{
if (c >= 65 && c < 91)
return (1);
else
return (0);
}
